#include <stdio.h>
// #include <stdlib.h>
// #include "AMessage.pb-c.h"

int main()
{
    // AMessage *msg = NULL;
    // msg = malloc(sizeof(*msg));
    // amessage__init(msg);

    printf("It works!!!\n");
    return 0;
}